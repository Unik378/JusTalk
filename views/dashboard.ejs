<!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/css/bootstrap.min.css"> -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
<link rel="stylesheet" href="/dashboard.css">

<%function formatDate(d,position='message'){

    const monthNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun","Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
    const today = new Date();

    var ampm;
    var hours = d.getHours();

    if( hours > 12 ){
        ampm = 'PM';
        hours = hours-12;
    }else{
        ampm = 'AM';
    }

    hours = ("0" + hours).slice(-2);

    if( position == 'message')
        return String(hours) + ':' + String(("0" + d.getMinutes()).slice(-2)) + ' ' + ampm + ' | ' + String(monthNames[d.getMonth()]) + ' ' + String(d.getDate()) + ',' + String(d.getFullYear());

    if( d.getFullYear() == today.getFullYear() && d.getMonth() == today.getMonth() )
    {
        if( d.getDate() == today.getDate() )
            return String(hours) + ':' + String(("0" + d.getMinutes()).slice(-2)) + ' ' + ampm;
        else if(d.getDate() + 1  == today.getDate())
            return 'yesterday';
        else 
            return  String(d.getDate()) + '/' + String(d.getMonth()+1) + '/' + String(d.getFullYear())
    }
} %> 

<div class="container mt-5 px-4">
    <div class="row rounded-lg overflow-hidden shadow">
        <!-- Users box-->
        <div class="col-5 px-0">
            <div style="height: 100%;" class="bg-white">

                <div style="display: flex; justify-content: space-between;" class="bg-gray px-4 py-2 bg-light">
                    <button style="padding: 0; box-shadow: none;" class="btn" onclick="cancelEdit()" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasExample2" aria-controls="offcanvasExample2">
                        <div style="display: flex;align-items: center;">
                            <div class="img-cropper-small">
                                <img src="<%= user.profilePicURL %> " alt="">
                            </div>
                            <p class="h5 ml-3 mb-0 py-1"><%= user.name %></p>
                        </div>
                    </button>
                    <div style="margin-top: 4px;" class="dropdown">
                        <a  type="text" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i style="width: 30px; text-align: center;" class="fa fa-ellipsis-h" aria-hidden="true"></i>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                          <li><a class="dropdown-item" href="/joinroom">Join Room</a></li>
                          <li><a class="dropdown-item" href="/createroom">Create Room</a></li>
                          <li><a class="dropdown-item" href="/users/logout">Logout</a></li>
                        </ul>
                      </div>
                </div>

                <div style="display: none;" id="noRoomMessage">
                    <p style="text-align: center;margin: 10px 2px;color: gray;">Room List empty.</p>
                </div>

                <div class="messages-box">
                    <div class="list-group rounded-0">
                        <% rooms.forEach(room => { %>
                            <% messages = room.messages %> 
                                <% lastMessage = messages.slice(-1)[0] %>
                                <% if( lastMessage.userSent.userId != 'bot' ){ %> 
                                    <div id="<%= room.name %>">
                                        <a href="#" class="list-group-item list-group-item-action list-group-item-light rounded-0">
                                            <div class="media">
                                                <div class="img-cropper-small">
                                                    <img src="/icon-avatar.png" alt="user">
                                                </div>
                                                <div class="media-body ml-4">
                                                    <div class="d-flex align-items-center justify-content-between mb-1">
                                                        <h5 class="mb-0"><%=room.name%></h5><small class="small"><%= formatDate(lastMessage.Date,'roomlist') %> </small>
                                                    </div>
                                                    <p class="text-muted mb-0 text-small"><%= lastMessage.userSent.name + ': ' + lastMessage.msg%> </p>
                                                </div>
                                            </div>
                                        </a>
                                    </div>
                                <% }else{ %>
                                    <div id="<%= room.name %>">
                                        <a href="#" class="list-group-item list-group-item-action list-group-item-light rounded-0">
                                            <div class="media">
                                                <div class="img-cropper-small">
                                                    <img src="/icon-avatar.png" alt="user">
                                                </div>
                                                <div class="media-body ml-4">
                                                    <div class="d-flex align-items-center justify-content-between mb-1">
                                                        <h5 class="mb-0"><%=room.name%></h5><small class="small"><%= formatDate(lastMessage.Date,'roomlist') %> </small>
                                                    </div>
                                                    <p class="text-muted mb-0 text-small"><%=lastMessage.msg%> </p>
                                                </div>
                                            </div>
                                        </a>
                                    </div>
                                <% } %> 
                        <% }); %>

                    </div>
                </div>
            </div>
        </div>
        <!-- Chat Box-->
        <div style="padding-left: 4px;padding-right: 0px;" id="chatarea" class="col-7">

            <div id="topBarChatBox" class="bg-gray px-4 py-2 bg-light" style="display: none; justify-content: space-between;">
                <button style="width: 100%;border: none;background: #f8f9fa !important;" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasExample" aria-controls="offcanvasExample">
                    <div id="roomName" class="h5 mb-0 py-1" ></div>
                </button>
            </div>

            <% rooms.forEach( room => { %>
                <div style="display: none;" id="<%= room.name %>ChatBox"  class="px-4 py-5 chat-box bg-white">
                    <% messages = room.messages %> 
                    <% messages.forEach(message => { %>
                        <% if( message.userSent.userId == 'bot'){ %> 
                            <div class="botMsgContainer">
                                <p class="botMsg"><%= message.msg %></p>
                            </div>
                        <% }else if( message.userSent.userId == user.id ) {%>
                            <div class="media w-50 ml-auto mb-3"><div class="media-body"><div class="bg-primary rounded py-2 px-3 mb-2"><p class="text-small mb-0 text-white"><%= message.msg %> </p></div><p class="small text-muted"><%= formatDate(message.Date) %></p></div></div>
                        <% }else{ %>
                            <div class="media w-50 mb-3">
                                <div class="img-cropper-small">
                                    <img src="/profilePics/<%=message.userSent.userId%>">
                                </div>
                                <div class="media-body ml-3">
                                    <p style="margin-bottom: 0;color: #3a6d99;"><%=message.userSent.name%></p>
                                    <div class="bg-light rounded py-2 px-3 mb-2">
                                        <p class="text-small mb-0 text-muted"><%=message.msg%></p>
                                    </div>
                                    <p class="small text-muted"><%=formatDate(message.Date)%></p>
                                </div>
                            </div>
                        <% } %> 
                    <% }); %> 
                </div>
            <% }); %>  

            <!-- Typing area -->
            <form id="typingArea" style="display: none;" action="#" class="bg-light">
                <div class="input-group">
                    <input id="msg" type="text" placeholder="Type a message" aria-describedby="button-addon2"
                        class="form-control rounded-0 border-0 py-4 bg-light">
                    <div class="input-group-append">
                        <button id="button-addon2" type="submit" class="btn btn-link"> <i
                                class="fa fa-paper-plane"></i></button>
                    </div>
                </div>
            </form>

            <div id="startMessage" style="display:flex; background-color: white; height: 100%; align-items: center; justify-content: center;">
                <p style="color: gray; font-size: large;">Please select a room to start chatting.</p>
            </div>

        </div>
    </div>
</div>
  
<div class="offcanvas offcanvas-end" data-bs-scroll="false" data-bs-backdrop="true" tabindex="-1" id="offcanvasExample" aria-labelledby="offcanvasExampleLabel">
    <div class="offcanvas-header">
        <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div style="display: flex; flex-direction: column; align-items: center;" id="roomDescription" class="offcanvas-body">
        <div>
            <img style="height: 150px; width: 150px;" src="/icon-avatar.png" alt="user" width="50" class="rounded-circle">
        </div>
        <div>
            <p><h5></h5></p>
        </div>
        <button id="leaveroom">
            <i class="fa fa-sign-out fa-2x" aria-hidden="true" style="margin-right: 15px;"></i>
            <span>Leave Room</span>
        </button>
        <div class="input-group" style="display: flex;align-items: center;background: #f8f9fa;padding-left: 10px;">
            <i style="color: #6c757d;" class="fa fa-search" aria-hidden="true"></i>
            <input type="text" id="myInput" onkeyup="myFunction()" placeholder="Search for members.." class="form-control rounded-0 border-0 py-4 bg-light">
        </div>
        <div style="width: 100%;">
        <ul id="myUL" class="list-group list-group-flush">
        </ul>
        </div>
    </div>
</div>

<div class="offcanvas offcanvas-start" data-bs-scroll="false" data-bs-backdrop="true" tabindex="-1" id="offcanvasExample2" aria-labelledby="offcanvasExample2Label">
    <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="offcanvasExample2Label">Profile</h5>
        <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close" onclick="cancelEdit()"></button>
    </div>
    <div class="offcanvas-body">
        <div>
            <form id="profilePicForm" action="/users/updateProfilePic" method="POST" enctype="multipart/form-data">
                <div style="display: flex;flex-direction: column; align-items: center;" class="form-group mb-0">
                    <div class="img-cropper-big">
                        <img id="output" src="<%= user.profilePicURL %>" alt="">
                        <h5 style="text-align: center;display: none;">Click to select photo</h5>
                    </div>
                    <label class="mb-0" for="profilePic">
                        <div id="editPic">
                            <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
                            Edit
                        </div>
                    </label>
                    <input type="file" id="profilePic" name="profilePic" class="form-control" accept="image/*" onchange="loadFile(event)" hidden/>
                </div>
                <button id="savePic" style="display: none;margin: auto;" type="submit" class="btn">
                    <img style="width: 40px;" src="/check.png" alt="">
                </button>
            </form>
        </div>

        <div>
            <form id="profileNameForm" action="/users/updateProfileName" method="POST">
                <div class="form-group">
                    <label style="color: rgb(0, 150, 0);" for="profileName">
                        Your Name
                    </label>
                    <div style="display: flex;align-items: center;">
                        <input type="text" id="profileName" name="profileName" class="form-control" value="<%= user.name %>" onfocus="this.selectionStart = this.selectionEnd = this.value.length;" disabled/>
                        <button id="saveName" style="display: none;margin: auto;" type="submit" class="btn">
                            <img style="width: 25px;" src="/check.png" alt="">
                        </button>
                        <button type="button" class="btn" id="editName" onclick="editProfileName()">
                            <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
                        </button>
                    </div>
                </div>
            </form>
        </div>

        <div class="form-group">
            <label style="color: rgb(0, 150, 0);" for="profileEmail">
                Your Email
            </label>
                <input type="email" id="profileEmail" name="profileEmail" class="form-control" value="<%= user.email %>" disabled/>
            </div>
        </div>
    </div>
</div>

<% var roomInfo = []%>
<% rooms.forEach( room => { %>
      <% roomInfo.push({ name: room.name, users: room.users, }); %>
<% }) %>


<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<!-- <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/js/bootstrap.bundle.min.js"></script> -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>
<script src="/socket.io/socket.io.js"></script>

<script>
    localStorage.setItem("user", '<%-JSON.stringify({name:user.name, email:user.email, id:user.id,rooms:roomInfo})%>')
    user = JSON.parse(localStorage.getItem("user"));
    topBarChatBox = document.getElementById('topBarChatBox');
    typingArea = document.getElementById('typingArea');
    startMessage = document.getElementById('startMessage');
    roomName = document.getElementById('roomName');
    onlineUsers = {};
    var currentRoom = null;
</script>

<script>
    
    function editProfileName()
    {
        document.getElementById('profileName').disabled = false;
        document.getElementById('editName').style.display = 'none';
        document.getElementById('saveName').style.display = 'block';
        document.getElementById('profileName').focus();
    }

    function cancelEdit()
    {
        document.getElementById('output').src = `/profilePics/${user.id}`;
        document.getElementById('profileName').value = user.name;

        document.getElementById('profileName').disabled = true;

        document.getElementById('savePic').style.display = 'none';
        document.getElementById('editPic').style.display = 'block'
        document.getElementById('saveName').style.display = 'none';
        document.getElementById('editName').style.display = 'block'
        editClicked = 0;
    }

    var loadFile = function(event) {
        isLoaded = 1;
        var reader = new FileReader();
        reader.onload = function(){
            var output = document.getElementById('output');
            output.src = reader.result;
        };
        reader.readAsDataURL(event.target.files[0]);

        document.getElementById('editPic').style.display = 'none';
        document.getElementById('savePic').style.display = 'block';
    };
  
</script>

<script>
    function myFunction() {
        // Declare variables
        var input, filter, ul, li, a, i, txtValue;
        input = document.getElementById('myInput');
        filter = input.value.toUpperCase();
        ul = document.getElementById("myUL");
        li = ul.getElementsByTagName('li');

        // Loop through all list items, and hide those who don't match the search query
        for (i = 0; i < li.length; i++) {
        txtValue = li[i].textContent || li[i].innerText;
        
        if (txtValue.toUpperCase().indexOf(filter) > -1) {
            li[i].style.display = "";
        } else {
            li[i].style.display = "none";
        }
    }
}
</script>

<script>
    function formatDate(d,position='message'){

        const monthNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun","Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
        const today = new Date();

        var ampm;
        var hours = d.getHours();

        if( hours > 12 ){
            ampm = 'PM';
            hours = hours-12;
        }else{
            ampm = 'AM';
        }

        hours = ("0" + hours).slice(-2);

        if( position == 'message')
            return String(hours) + ':' + String(("0" + d.getMinutes()).slice(-2)) + ' ' + ampm + ' | ' + String(monthNames[d.getMonth()]) + ' ' + String(d.getDate()) + ',' + String(d.getFullYear());

        if( d.getFullYear() == today.getFullYear() && d.getMonth() == today.getMonth() )
        {
            if( d.getDate() == today.getDate() )
                return String(hours) + ':' + String(("0" + d.getMinutes()).slice(-2)) + ' ' + ampm;
            else if(d.getDate() + 1  == today.getDate())
                return 'yesterday';
            else 
                return  String(d.getDate()) + '/' + String(d.getMonth()+1) + '/' + String(d.getFullYear())
        }
    }
</script>

<script>
    const socket = io();

    socket.emit('joinRooms',{rooms:user.rooms, userId: user.id});

    socket.on('online-users', data => {
        onlineUsers = data.onlineUsers;
        console.log('In socket ',onlineUsers)
    });

    socket.on('online' , data => {
        console.log('online')
        onlineUsers[data.userId] = 1;

        const elem = document.getElementById(data.userId);
        if( elem )
        {
            elem.getElementsByTagName('i')[0].style.color = 'green';
        }
    })

    socket.on('offline' , id => {
        console.log('offline')
        if( id != user.id )
        {
            onlineUsers[id] = 0;
            const elem = document.getElementById(id);
            if( elem )
            {
                elem.getElementsByTagName('i')[0].style.color = 'red';
            }
        }
    })

    socket.on('user-joined' , (data) => {
        document.getElementById(data.room+'ChatBox').innerHTML += `<div class="botMsgContainer"><p class="botMsg">${data.userName} joined</p></div>`;

        document.getElementById(currentRoom).getElementsByTagName('p')[0].innerText = `${data.userName} joined`;
        displayDate = formatDate(new Date(data.Date),'roomlist');
        document.getElementById(currentRoom).getElementsByTagName('small')[0].innerText = `${displayDate}`;

        onlineUsers[data.userId] = 1;

        user.rooms[user.rooms.findIndex(x => x.name == data.room)].users.push({userId:data.userId,name:data.userName});
        
        if( currentRoom == data.room )
        {
            document.getElementById('myUL').innerHTML +=  `<li id="${data.userId}" class="list-group-item">
                                                            <div class="img-cropper-small">
                                                                <img src="/profilePics/${data.userId}" alt="user">
                                                            </div>
                                                            <i style="position: absolute;left: 52px;top: 43px;color: green;"class="fa fa-circle" aria-hidden="true"></i>
                                                            ${data.userName}
                                                            </li>`;
        }

        console.log(`${data.userName} joined ${data.room}`);
    })

    typingArea.addEventListener('submit', (e) => {

        e.preventDefault();

        //Get message text 
        const msg = e.target.elements.msg.value;

        const message = {
            msg: msg,
            userSent :{
                userId : user.id,
                name: user.name
            },
            Date: new Date()
        }

        //emitting the sent message
        socket.emit('sent-message', { message:message,room:currentRoom });

        displayDate = formatDate(message.Date);
        displayDateRoomList = formatDate(message.Date,'roomlist')

        chatBox = document.getElementById(currentRoom+'ChatBox');
        chatBox.innerHTML += `<div class="media w-50 ml-auto mb-3"><div class="media-body"><div class="bg-primary rounded py-2 px-3 mb-2"><p class="text-small mb-0 text-white">${message.msg}</p></div><p class="small text-muted">${displayDate}</p></div></div>`;
        
        document.getElementById(currentRoom).getElementsByTagName('p')[0].innerText = `${user.name}: ${msg}`;
        document.getElementById(currentRoom).getElementsByTagName('small')[0].innerText = `${displayDateRoomList}`;

        firstChild = document.getElementById(currentRoom).parentElement.childNodes[0];
        document.getElementById(currentRoom).parentElement.insertBefore(document.getElementById(currentRoom),firstChild);

        shouldScroll = chatBox.scrollTop + chatBox.clientHeight === chatBox.scrollHeight;
        if (!shouldScroll) {
            scrollToBottom(chatBox);
        }
        
        //making the message box blank 
        e.target.elements.msg.value = '';
    });

    socket.on('message' , (data) => {
        chatBox = document.getElementById(data.room+'ChatBox');

        displayDate = formatDate(new Date(data.message.Date));
        displayDateRoomList = formatDate(new Date(data.message.Date),'roomlist')

        chatBox.innerHTML +=`<div class="media w-50 mb-3">
                                <div class="img-cropper-small">
                                    <img src="/profilePics/${data.message.userSent.userId}" alt="user">
                                </div>
                                <div class="media-body ml-3">
                                    <p style="margin-bottom: 0;color: #3a6d99;">${data.message.userSent.name}</p>
                                    <div class="bg-light rounded py-2 px-3 mb-2">
                                        <p class="text-small mb-0 text-muted">${data.message.msg}</p>
                                    </div>
                                    <p class="small text-muted">${displayDate}</p>
                                </div>
                            </div>`;

        document.getElementById(data.room).getElementsByTagName('p')[0].innerText = `${data.message.userSent.name}: ${data.message.msg}`;
        document.getElementById(data.room).getElementsByTagName('small')[0].innerText = `${displayDateRoomList}`;

        firstChild = document.getElementById(data.room).parentElement.childNodes[0];
        document.getElementById(data.room).parentElement.insertBefore(document.getElementById(data.room),firstChild);

        shouldScroll = chatBox.scrollTop + chatBox.clientHeight === chatBox.scrollHeight;
        if (!shouldScroll) {
            scrollToBottom(chatBox);
        }

    });

    document.getElementById('leaveroom').addEventListener('click' , (e) => {
        e.preventDefault();

        user.rooms.splice(user.rooms.findIndex( x => x.name == currentRoom ),1);
        if(user.rooms.length == 0 )
            document.getElementById('noRoomMessage').style.display = 'block';

        topBarChatBox.style.display = 'none';
        typingArea.style.display = 'none';
        startMessage.style.display = 'flex';

        document.getElementById(currentRoom).remove();
        document.getElementById(currentRoom+'ChatBox').remove();

        socket.emit('leave-room' , {id:user.id, name: user.name, room:currentRoom});

        currentRoom = null;
    });

    socket.on('user-left', data => {
        console.log('user left ', data);
        document.getElementById(data.room+'ChatBox').innerHTML += `<div class="botMsgContainer"><p class="botMsg">${data.name} left</p></div>`;

        document.getElementById(currentRoom).getElementsByTagName('p')[0].innerText = `${data.name} left`;
        displayDate = formatDate(new Date(data.Date),'roomlist');
        document.getElementById(currentRoom).getElementsByTagName('small')[0].innerText = `${displayDate}`;

        roomIndex = user.rooms.findIndex(x => x.name == data.room);
        userIndex = user.rooms[roomIndex].users.findIndex( x => x.userId == data.id);
        user.rooms[roomIndex].users.splice(userIndex,1);


        if( currentRoom == data.room )
        {
            elem = document.getElementById(data.id);
            if(elem)
            {
                elem.remove();
            }
        }
    })

</script>

<script>
    function scrollToBottom(chatBox) {
        chatBox.scrollTop = chatBox.scrollHeight;
    }

    if(user.rooms.length == 0 )
        document.getElementById('noRoomMessage').style.display = 'block';

    user.rooms.forEach(room => {

        document.getElementById(room.name).addEventListener('mousedown', (e) => {
            e.preventDefault();

            currentRoom = document.getElementById(room.name).id;
            console.log(currentRoom)
            topBarChatBox.style.display = 'flex';
            typingArea.style.display = 'block';
            startMessage.style.display = 'none';
            roomDescription = document.getElementById('roomDescription');
            

            user.rooms.forEach(room1 => {
                document.getElementById(room1.name+'ChatBox').style.display = 'none'
                document.getElementById(room1.name).getElementsByTagName('a')[0].classList.remove('active');
                document.getElementById(room1.name).getElementsByTagName('a')[0].classList.remove('text-white');
                document.getElementById(room1.name).getElementsByTagName('a')[0].classList.add('list-group-item-light');

                document.getElementById(room1.name).getElementsByTagName('p')[0].classList.add('text-muted');
            });

            document.getElementById(room.name+'ChatBox').style.display = 'block'
            scrollToBottom(document.getElementById(room.name+'ChatBox'))
            roomName.innerText =  room.name;

            document.getElementById(room.name).getElementsByTagName('a')[0].classList.add('active');
            document.getElementById(room.name).getElementsByTagName('a')[0].classList.add('text-white');
            document.getElementById(room.name).getElementsByTagName('a')[0].classList.remove('list-group-item-light');

            document.getElementById(room.name).getElementsByTagName('p')[0].classList.remove('text-muted');

            roomDescription.getElementsByTagName('h5')[0].innerText = room.name;
            myUL = document.getElementById('myUL');
            myUL.innerHTML = '';

            console.log(onlineUsers);
            room.users.forEach( member => {
                if( onlineUsers[member.userId])
                {
                    myUL.innerHTML +=  `<li id="${member.userId}" class="list-group-item">
                                        <div class="img-cropper-small">
                                            <img src="/profilePics/${member.userId}" alt="user">
                                        </div>
                                        <i style="position: absolute;left: 52px;top: 43px;color: green;"class="fa fa-circle" aria-hidden="true"></i>
                                        ${member.name}
                                    </li>`;
                }
                else
                {
                    myUL.innerHTML +=  `<li id="${member.userId}" class="list-group-item">
                                        <div class="img-cropper-small">
                                            <img src="/profilePics/${member.userId}" alt="user">
                                        </div>
                                        <i style="position: absolute;left: 52px;top: 43px;color: red;"class="fa fa-circle" aria-hidden="true"></i>
                                        ${member.name}
                                    </li>`;
                }
            })
        })
    });
</script>